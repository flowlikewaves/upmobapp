<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mobappg4v2.Views.ShippingAddressView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:Mobappg4v2.Converters"
    x:Name="AddressPage"
    Title="Address">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBooleanConverter x:Key="InverseBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid Margin="0" BackgroundColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource SecondaryDark}}">
        <CollectionView IsVisible="{Binding IsLoaded}" ItemsSource="{Binding Addressess}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" VerticalItemSpacing="32" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Style="{StaticResource CardFrameStyle}" Margin="0">
                        <StackLayout Spacing="10">
                            <Label
                                Style="{StaticResource HeaderLabelStyle}"
                                FontSize="18"
                                Text="{Binding AddressType}" />
                            <Grid ColumnDefinitions="*,18,24">
                                <Label
                                    Grid.Column="0"
                                    FontSize="14"
                                    HorizontalOptions="Start"
                                    Text="{Binding FullAddress}"
                                    TextColor="{StaticResource Black}" />
                                <Label
                                    Grid.Column="2"
                                    FontFamily="MaterialIcon"
                                    FontSize="24"
                                    HorizontalOptions="Center"
                                    IsVisible="{Binding IsSelected}"
                                    Text="&#xf133;"
                                    TextColor="{StaticResource Primary}"
                                    VerticalOptions="Center" />
                            </Grid>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={x:Reference AddressPage}, Path=BindingContext.SelectAddressCommand}" CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <ActivityIndicator
            HeightRequest="45"
            HorizontalOptions="Center"
            IsRunning="{Binding IsLoaded, Converter={StaticResource InverseBoolConverter}}"
            VerticalOptions="Center"
            WidthRequest="45"
            Color="{StaticResource Primary}" />
    </Grid>
</ContentPage>