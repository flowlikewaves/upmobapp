<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mobappg4v2.Views.AllProductView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:Mobappg4v2.Converters"
    xmlns:vm="clr-namespace:Mobappg4v2.ViewModel"
    x:Name="AllProductPage"
    Title="All Products">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBooleanConverter x:Key="InverseBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid BackgroundColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource SecondaryDark}}">
        <CollectionView
            Margin="16,12"
            IsVisible="{Binding IsLoaded}"
            ItemsSource="{Binding Products}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="16"
                    Orientation="Vertical"
                    Span="2"
                    VerticalItemSpacing="16" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Style="{StaticResource CardFrameStyle}" Margin="0">
                        <StackLayout HorizontalOptions="FillAndExpand">
                            <Image
                                Aspect="AspectFit"
                                HeightRequest="180"
                                WidthRequest="140"
                                Source="{Binding ImageUrl}" />
                            <Label
                                Style="{StaticResource HeaderLabelStyle}"
                                HorizontalOptions="Center"
                                Text="{Binding Name}" />
                            <Label
                                FontSize="12"
                                HorizontalOptions="Center"
                                Text="{Binding BrandName}"
                                TextColor="{StaticResource SecondaryTextColor}"
                                VerticalOptions="Start" />
                            <Label
                                FontSize="16"
                                HorizontalOptions="Center"
                                Text="{Binding Price, StringFormat='${0:F2}'}"
                                TextColor="{StaticResource Primary}" />
                        </StackLayout>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={x:Reference AllProductPage}, Path=BindingContext.SelectProductCommand}" CommandParameter="{Binding .}" />
                        </Frame.GestureRecognizers>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <ActivityIndicator
            HeightRequest="45"
            HorizontalOptions="Center"
            IsRunning="{Binding IsLoaded, Converter={StaticResource InverseBoolConverter}}"
            VerticalOptions="Center"
            WidthRequest="45"
            Color="{StaticResource Primary}" />
    </Grid>
</ContentPage>